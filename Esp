local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Camera = Workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

-- ตรวจสอบว่าโปรแกรมรันสคริปต์รองรับระบบ Drawing หรือไม่
if not Drawing then
    print("Executor ของคุณไม่รองรับ Drawing Library!")
    return
end

-- ป้องกันเปิดซ้ำ (ลบอันเก่าทิ้งถ้ามี)
if CoreGui:FindFirstChild("ESPToggleGui") then
    CoreGui.ESPToggleGui:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ESPToggleGui"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

-- กรอบหลัก UI
local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 220, 0, 125)
Frame.Position = UDim2.new(0.5, -110, 0.25, 0)
Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
Frame.BorderSizePixel = 0
Frame.Active = true
Frame.Draggable = true
Frame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = Frame

-- เส้นขอบ UI
local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(60, 60, 80)
UIStroke.Thickness = 1.5
UIStroke.Parent = Frame

-- หัวข้อ
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -20, 0, 25)
Title.Position = UDim2.new(0, 15, 0, 5)
Title.BackgroundTransparency = 1
Title.Text = "👁️ ระบบแสดงตำแหน่ง (ESP)"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Frame

-- ชื่อแมพย่อย
local SubTitle = Instance.new("TextLabel")
SubTitle.Size = UDim2.new(1, -20, 0, 15)
SubTitle.Position = UDim2.new(0, 15, 0, 23)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "ชุมทางเสือ"
SubTitle.TextColor3 = Color3.fromRGB(150, 150, 170)
SubTitle.TextSize = 11
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextXAlignment = Enum.TextXAlignment.Left
SubTitle.Parent = Frame

-- ป้ายสถานะ
local StatusFrame = Instance.new("Frame")
StatusFrame.Size = UDim2.new(0, 75, 0, 22)
StatusFrame.Position = UDim2.new(1, -90, 0, 12)
StatusFrame.BackgroundColor3 = Color3.fromRGB(50, 30, 30)
StatusFrame.BorderSizePixel = 0
StatusFrame.Parent = Frame

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 6)
StatusCorner.Parent = StatusFrame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(1, 0, 1, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "OFF"
StatusLabel.TextColor3 = Color3.fromRGB(255, 90, 90)
StatusLabel.TextSize = 11
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.Parent = StatusFrame

-- ปุ่มเปิด-ปิด ESP
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(1, -30, 0, 38)
ToggleButton.Position = UDim2.new(0, 15, 0, 68)
ToggleButton.BackgroundColor3 = Color3.fromRGB(230, 60, 60)
ToggleButton.Text = "เปิดใช้งาน ESP"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 14
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = Frame

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 8)
ButtonCorner.Parent = ToggleButton

local espEnabled = false
local espCache = {}

-- ฟังก์ชันลบ ESP ของผู้เล่นที่ออกจากเกม
local function removeEsp(player)
    if espCache[player] then
        for _, obj in pairs(espCache[player]) do
            obj:Remove()
        end
        espCache[player] = nil
    end
end

-- ฟังก์ชันสร้าง ESP สำหรับผู้เล่นใหม่
local function createEsp(player)
    if espCache[player] then return end
    local box = Drawing.new("Square")
    box.Visible = false
    box.Color = Color3.fromRGB(0, 255, 120)
    box.Thickness = 1.5
    box.Filled = false

    local text = Drawing.new("Text")
    text.Visible = false
    text.Color = Color3.fromRGB(255, 255, 255)
    text.Size = 13
    text.Center = true
    text.Outline = true

    espCache[player] = {Box = box, Text = text}
end

-- ตั้งค่าผู้เล่นที่มีอยู่แล้วในเซิร์ฟเวอร์
for _, p in ipairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then
        createEsp(p)
    end
end

Players.PlayerAdded:Connect(function(p)
    if p ~= LocalPlayer then
        createEsp(p)
    end
end)

Players.PlayerRemoving:Connect(function(p)
    removeEsp(p)
end)

-- ลูปอัปเดตตำแหน่งแบบเรียลไทม์ (ไม่มีบัคกินแรม)
RunService.RenderStepped:Connect(function()
    if not espEnabled then
        for _, cache in pairs(espCache) do
            cache.Box.Visible = false
            cache.Text.Visible = false
        end
        return
    end

    for player, cache in pairs(espCache) do
        local char = player.Character
        local rootPart = char and char:FindFirstChild("HumanoidRootPart")
        local humanoid = char and char:FindFirstChildOfClass("Humanoid")

        if char and rootPart and humanoid and humanoid.Health > 0 then
            local vector, onScreen = Camera:WorldToViewportPoint(rootPart.Position)
            if onScreen then
                local head = char:FindFirstChild("Head")
                local topVector = head and Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0)) or vector
                local legVector = Camera:WorldToViewportPoint(rootPart.Position - Vector3.new(0, 3, 0))

                local height = math.abs(topVector.Y - legVector.Y)
                local width = height / 2

                -- อัปเดตกล่องกรอบสี่เหลี่ยม
                cache.Box.Size = Vector2.new(width, height)
                cache.Box.Position = Vector2.new(vector.X - width / 2, topVector.Y)
                cache.Box.Visible = true

                -- คำนวณระยะห่าง (เมตร) และชื่อ
                local distance = math.floor((Camera.CFrame.Position - rootPart.Position).Magnitude)
                cache.Text.Text = player.Name .. " [" .. distance .. "m]"
                cache.Text.Position = Vector2.new(vector.X, topVector.Y - 18)
                cache.Text.Visible = true
            else
                cache.Box.Visible = false
                cache.Text.Visible = false
            end
        else
            cache.Box.Visible = false
            cache.Text.Visible = false
        end
    end
end)

-- ปุ่มกดสลับเปิด-ปิด
ToggleButton.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    if espEnabled then
        StatusLabel.Text = "ON"
        StatusLabel.TextColor3 = Color3.fromRGB(90, 255, 120)
        StatusFrame.BackgroundColor3 = Color3.fromRGB(20, 50, 30)
        ToggleButton.Text = "ปิดใช้งาน ESP"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(50, 205, 90)
    else
        StatusLabel.Text = "OFF"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 90, 90)
        StatusFrame.BackgroundColor3 = Color3.fromRGB(50, 30, 30)
        ToggleButton.Text = "เปิดใช้งาน ESP"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(230, 60, 60)
        for _, cache in pairs(espCache) do
            cache.Box.Visible = false
            cache.Text.Visible = false
        end
    end
end)
